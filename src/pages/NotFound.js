import React from "react";
import { DefaultTemplate } from "@templates";

const NotFound = () => {
  return (
    <DefaultTemplate>
      <h1>Not Found</h1>
    </DefaultTemplate>
  );
};

export default NotFound;
